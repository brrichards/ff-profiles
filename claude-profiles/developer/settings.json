{
	"$schema": "https://json.schemastore.org/claude-code-settings.json",
	"permissions": {
		"deny": [
			"Bash(git push --force*)",
			"Bash(git push * --force*)",
			"Bash(rm -rf /*)",
			"Bash(git reset --hard*)",
			"Bash(git checkout -- .)",
			"Bash(git clean -f*)"
		]
	}
}
